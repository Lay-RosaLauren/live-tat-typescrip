{
  "name": "live-tat-typescript",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "cleanup": "rm *.js && rm **/*.js || true",
    "compile": "tsc index.ts",
    "cleanup:and:compile": "npm run cleanup && npm run compile",
    "execute": "node index.js"
  },
  "author": "Lay-RosaLauren",
  "license": "MIT",
  "dependencies": {
    "typescript": "^4.5.5"
  }
}
